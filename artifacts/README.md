# Artifacts (Gitignored)

This folder stores large and/or binary outputs (audio, exports, intermediate files).

## Policy
- Artifacts are not committed to git.
- Canonical references to artifacts (paths, URLs, metadata) should be stored in SQLite and/or Markdown notes.

## Example
- `artifacts/podcasts/YYYY-MM-DD--slug.mp3`
