# Projections (Generated)

Files under `brain/projections/` are generated outputs from SQLite (`state/brain.db`).

## Rules
- Treat as read-only.
- If output is wrong, update SQLite or projection definitions under `brain/db/projections/` and regenerate.
- Regenerate with: `make projections`
